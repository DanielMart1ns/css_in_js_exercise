import { HeroContainer, HeroTitle } from './styles'

const Hero = () => (
  <HeroContainer>
    <div>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </div>
  </HeroContainer>
)

export default Hero
